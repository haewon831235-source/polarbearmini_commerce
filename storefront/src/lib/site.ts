// Canonical site URL. Override per-environment with NEXT_PUBLIC_SITE_URL;
// defaults to the production domain (purchased for the Polaris Atelier store).
export const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://polarisatelierkorea.com";
